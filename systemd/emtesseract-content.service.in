[Unit]
Description=emTesseract content worker (write_content)
After=network.target postgresql.service

[Service]
Type=simple
User=@USER@
WorkingDirectory=@PROJECT_ROOT@
EnvironmentFile=@PROJECT_ROOT@/.env
ExecStart=/usr/bin/env node workers/content-worker.mjs
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
